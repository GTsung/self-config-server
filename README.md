~~~~~~

    配置中心:
        原理: 1.將@Value注解的屬性名稱及其所在類放入到緩存中(map),將@Value注解的value值為鍵，之前的map為值放入到緩存中(map);
              2.將配置文件的鍵值對進行解析，放入到緩存中(map);
              3.當修改配置文件時，將新的配置文件進行解析放入到map中，與舊map進行比對，如果新map中存在key且value不相等，修改舊map，
           並從第一步中拿出此key的類與屬性，通過反射將這個屬性的值重新set;

~~~~~~